# ============================================================
# Flash-Searcher 环境配置模板
# 使用方法: cp .env.example .env  然后填入真实的 key
# ============================================================

# === 核心 LLM 配置 (必填) ===
DEFAULT_MODEL="qwen3-max"
DEFAULT_JUDGE_MODEL="qwen3-max"
OPENAI_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
OPENAI_API_BASE="https://dashscope.aliyuncs.com/compatible-mode/v1"
OPENAI_API_KEY=""                # 阿里云 DashScope API Key (https://dashscope.console.aliyun.com/)

# === DMX 备用 API (可选) ===
DMX_MODEL="qwen3-max"
DMX_API_BASE="https://www.dmxapi.cn/v1"
DMX_API_KEY=""                   # DMX API Key

# === 进化模块模型 (可选，默认同 DEFAULT_MODEL) ===
ANALYSIS_MODEL="qwen3-max"
GENERATION_MODEL="qwen3-max"

# === 搜索与爬虫 (必填，agent 运行需要) ===
SERPER_API_KEY=""                # Serper 搜索 API Key (https://serper.dev/)
WEB_ACCESS_PROVIDER="crawl4ai"  # 爬虫引擎: "crawl4ai"(免费) 或 "jina"(需 JINA_API_KEY)

# === GitHub API (Task 9 等 GitHub 查询任务需要) ===
GITHUB_TOKEN=""                  # GitHub Personal Access Token (https://github.com/settings/tokens)

# === HuggingFace (下载 GAIA 数据集需要) ===
HF_TOKEN=""                      # HuggingFace Access Token (https://huggingface.co/settings/tokens)
